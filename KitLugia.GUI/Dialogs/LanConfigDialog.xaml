<Window x:Class="KitLugia.GUI.Dialogs.LanConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Servidor LAN" Height="200" Width="350"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- T&#xED;tulo -->
        <TextBlock Grid.Row="0" Text="&#x1F310; Servidor LAN" FontSize="16" FontWeight="Bold" 
                   Foreground="{DynamicResource AccentColor}" Margin="0,0,0,15" HorizontalAlignment="Center"/>
        
        <!-- Porta -->
        <TextBlock Grid.Row="1" Text="Porta:" FontSize="12" 
                   Foreground="#AAA" Margin="0,0,0,3"/>
        <TextBox x:Name="TxtPort" Grid.Row="2" Text="25565" Height="30" 
                 Background="#252525" Foreground="White" BorderBrush="#FFD700" BorderThickness="2"
                 Padding="8,5" FontSize="12" Margin="0,0,0,15"/>
        
        <!-- Bot&#xF5;es -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="80" Height="30" Margin="0,0,10,0"
                    Background="Transparent" BorderBrush="#666" Foreground="#AAA"
                    Click="BtnCancel_Click"/>
            <Button Content="Ativar" Width="80" Height="30"
                    Background="{DynamicResource AccentColor}" Foreground="White" BorderThickness="0"
                    Click="BtnActivate_Click"/>
        </StackPanel>
    </Grid>
</Window>
