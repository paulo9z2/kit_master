<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="KitLugia.GUI.Pages.ServerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1050"
      Title="Servidor Público"
      TextElement.FontFamily="Segoe UI Variable Display, Segoe UI, Segoe UI Emoji, Segoe UI Symbol, Arial"
      Unloaded="Page_Unloaded">

    <Page.Resources>
        <!-- ESTILO BOTÃO DESATIVAR (VERMELHO) -->
        <Style x:Key="StopServerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#C0392B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#E74C3C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bdr" Property="Background" Value="#E74C3C"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="bdr" Property="Background" Value="#922B21"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecButton" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/><Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#FFD700"/><Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#3A3A3A"/><Setter Property="BorderBrush" Value="#FFA500"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="25,25,25,40" MaxWidth="950">

            <!-- CABEÇALHO -->
            <TextBlock Text="🌐 Servidor Minecraft Público" FontSize="28" FontWeight="SemiBold" Foreground="White"/>
            <TextBlock Text="Compartilhe seu mundo Minecraft SEM precisar configurar roteador ou usar Hamachi." Foreground="#A0A0A0" Margin="0,5,0,20" TextWrapping="Wrap"/>

            <!-- STATUS -->
            <Border Background="#15FFFFFF" BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
                <StackPanel Margin="15">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#15FFFFFF">
                            <TextBlock x:Name="StatusIcon" Text="🌐" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                            <TextBlock x:Name="StatusText" Text="Pronto" FontSize="17" FontWeight="Bold" Foreground="White"/>
                            <TextBlock x:Name="StatusDetail" Text="Abra o Minecraft e clique em Detectar" FontSize="11" Foreground="#A0A0A0"/>
                        </StackPanel>
                        <Button x:Name="BtnToggle" Grid.Column="2" Width="170" Height="40" Style="{StaticResource GoldButtonStyle}" Content="▶ Iniciar" Click="BtnToggle_Click" FontSize="12"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- CONFIGURAÇÃO -->
            <Border Background="#15FFFFFF" BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
                <StackPanel Margin="15">
                    <TextBlock Text="⚙️ Configuração do Servidor" FontSize="16" FontWeight="Bold" Foreground="#FFD700"/>
                    <TextBlock Text="Detecte automaticamente a porta do Minecraft LAN e compartilhe com amigos." Foreground="#A0A0A0" FontSize="12" Margin="0,3,0,12"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🎮" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="1" Text="Porta do Minecraft LAN" FontSize="13" Foreground="White" VerticalAlignment="Center"/>
                        <TextBox x:Name="TxtMinecraftPort" Grid.Column="2" Height="30" Text="" Style="{StaticResource DarkTextBoxStyle}" FontSize="12" Background="#1A1A1A" Foreground="White"/>
                        <Button x:Name="BtnDetect" Grid.Column="3" Content="🔍 Detectar" Style="{StaticResource SecButton}" Width="100" Height="30" Margin="10,0,0,0" Click="BtnDetect_Click"/>
                    </Grid>
                    
                    <TextBlock Text="💡 Dica: Detecte automaticamente ou digite manualmente a porta que aparece no Minecraft (ESC → 'Abrir para LAN')." Foreground="#808080" FontSize="11" FontStyle="Italic" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- ENDEREÇO PÚBLICO -->
            <Border Background="#15FFFFFF" BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
                <StackPanel Margin="15">
                    <TextBlock Text="🌍 Endereço para Amigos" FontSize="14" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="TxtEndpoint" Text="Aguardando detecção..." FontSize="18" FontWeight="Bold" Foreground="#00FF00" Background="#1A1A1A" Padding="10,6" FontFamily="Consolas" Margin="0,6,0,6"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📋 Copie e envie para seus amigos conectarem" Foreground="#808080" FontSize="11" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="BtnTunnel" Content="🚀 Tunnel" Style="{StaticResource SecButton}" Width="80" Height="30" Margin="0,0,5,0" Click="BtnTunnel_Click"/>
                            <Button x:Name="BtnTest" Content="🧪 Testar" Style="{StaticResource SecButton}" Width="80" Height="30" Margin="0,0,5,0" Click="BtnTest_Click"/>
                            <Button x:Name="BtnCopy" Content="📋 Copiar" Style="{StaticResource SecButton}" Width="100" Height="30" Margin="0,0,0,0" Click="BtnCopy_Click"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- LOG -->
            <Border Background="#15FFFFFF" BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10">
                <StackPanel Margin="12">
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📋 Log" FontSize="14" FontWeight="Bold" Foreground="#FFD700"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock x:Name="TxtLogCount" Text="0" FontSize="10" Foreground="#808080" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="📋 Copiar" Style="{StaticResource SecButton}" Width="70" Height="25" FontSize="10" Click="BtnCopyLog_Click" Margin="0,0,4,0"/>
                            <Button Content="🗑️" Style="{StaticResource SecButton}" Width="30" Height="25" FontSize="10" Click="BtnClearLog_Click"/>
                        </StackPanel>
                    </Grid>
                    <Border Background="#1E1E1E" BorderBrush="#404040" BorderThickness="1" CornerRadius="4">
                        <ScrollViewer x:Name="LogScroller" Height="150" VerticalScrollBarVisibility="Auto" Margin="1">
                            <TextBlock x:Name="TxtLog" Text="🌐 Pronto. Abra o Minecraft e clique em Detectar." FontSize="10" Foreground="#E0E0E0" FontFamily="Consolas" TextWrapping="NoWrap" LineHeight="14"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>

            <!-- GUIA -->
            <Border Background="#15FFFFFF" BorderBrush="#33FFD700" BorderThickness="1" CornerRadius="8">
                <StackPanel Margin="15">
                    <TextBlock Text="📖 Guia Rápido" FontSize="15" FontWeight="Bold" Foreground="#FFD700" Margin="0,0,0,10"/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#C0C0C0" LineHeight="18">
                        <Run Text="🎮 PASSO 1: "/><Run Text="Abra o Minecraft e entre no seu mundo" FontWeight="Bold" Foreground="White"/><LineBreak/>
                        <Run Text="🎮 PASSO 2: "/><Run Text="Pressione ESC → 'Abrir para LAN'" FontWeight="Bold" Foreground="White"/><LineBreak/>
                        <Run Text="🎮 PASSO 3: "/><Run Text="Clique em '🔍 Detectar' aqui no KitLugia" FontWeight="Bold" Foreground="White"/><LineBreak/>
                        <Run Text="🎮 PASSO 4: "/><Run Text="Clique em '▶ Iniciar' para obter endereço público" FontWeight="Bold" Foreground="White"/><LineBreak/>
                        <Run Text="🎮 PASSO 5: "/><Run Text="Copie e envie o endereço para seus amigos" FontWeight="Bold" Foreground="White"/><LineBreak/>
                        <LineBreak/>
                        <Run Text="🌐 Seus amigos conectarão diretamente usando: "/><Run Text="SEU_IP:PORTA_MINECRAFT" FontWeight="Bold" Foreground="#00FF00"/><LineBreak/>
                        <Run Text="✅ Sem configuração de roteador, sem Hamachi, sem complicações!" Foreground="#808080" FontStyle="Italic"/>
                    </TextBlock>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>