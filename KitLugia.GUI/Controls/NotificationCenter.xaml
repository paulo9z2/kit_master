<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="KitLugia.GUI.Controls.NotificationCenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="350">

    <UserControl.Resources>
        <!-- &#x1F4CD; ANIMA&#xC7;&#xC3;O 3: Slide do NotificationCenter (0.3s) - NotificationCenter.xaml linha ~12-36 -->
        <!-- Otimizado: BlurRadius reduzido de 30 para 15 para melhorar performance -->
        <Storyboard x:Key="OpenMenu">
            <DoubleAnimation Storyboard.TargetName="SidePanel"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="350" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="OverlayBg"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>

        <Storyboard x:Key="CloseMenu">
            <DoubleAnimation Storyboard.TargetName="SidePanel"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             To="350" Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="OverlayBg"
                             Storyboard.TargetProperty="Opacity"
                             To="0" Duration="0:0:0.25"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <!-- Fundo Escuro (Clique fecha) -->
        <Border x:Name="OverlayBg" Background="#44000000" Opacity="0" MouseDown="OverlayBg_MouseDown"/>

        <!-- Painel Lateral -->
        <Border x:Name="SidePanel" Width="340" HorizontalAlignment="Right" 
                Background="#101010" BorderBrush="#333" BorderThickness="1,0,0,0">
            <Border.RenderTransform>
                <TranslateTransform X="350"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="-5" Opacity="0.6"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="*"/>
                    <!-- Lista -->
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#x1F514;" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="Notifica&#xE7;&#xF5;es" Foreground="White" FontWeight="Bold" FontSize="18"/>
                    </StackPanel>

                    <!-- Bot&#xE3;o Limpar Tudo -->
                    <Button Grid.Column="1" Content="Limpar tudo" Click="BtnClear_Click" 
                            Background="Transparent" Foreground="#FF6F61" BorderThickness="0" 
                            FontSize="12" Cursor="Hand" FontWeight="SemiBold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent" Padding="5">
                                    <!-- ADICIONADO x:Name AQUI PARA O TRIGGER FUNCIONAR -->
                                    <TextBlock x:Name="TxtContent" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="White"/>
                                        <!-- CORRE&#xC7;&#xC3;O: Aplica o sublinhado no TextBlock alvo, n&#xE3;o no Bot&#xE3;o -->
                                        <Setter TargetName="TxtContent" Property="TextDecorations" Value="Underline"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- LISTA -->
                <ListView x:Name="ListNotifications" Grid.Row="1" 
                          Background="Transparent" BorderThickness="0" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <!-- CARD DA NOTIFICA&#xC7;&#xC3;O -->
                            <Border Background="#1A1A1A" CornerRadius="6" BorderThickness="0,0,0,1" BorderBrush="#333">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Faixa Colorida Lateral (Baseada no Tipo) -->
                                    <Border Grid.Column="0" Background="{Binding ColorHex}" CornerRadius="6,0,0,6"/>

                                    <Grid Grid.Column="1" Margin="12,10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- &#xCD;cone -->
                                        <TextBlock Text="{Binding Icon}" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                        <!-- T&#xED;tulo e Hora -->
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Title}" Foreground="{Binding ColorHex}" FontWeight="Bold" FontSize="13"/>
                                            <TextBlock Text=" &#x2022; " Foreground="#555"/>
                                            <TextBlock Text="{Binding TimeString}" Foreground="#777" FontSize="11" VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <!-- Bot&#xE3;o FECHAR (X) -->
                                        <Button Grid.Column="2" Content="&#x2715;" Click="BtnRemoveItem_Click" Tag="{Binding}"
                                                Background="Transparent" BorderThickness="0" Foreground="#555" 
                                                FontSize="10" Width="20" Height="20" Cursor="Hand" VerticalAlignment="Top" Margin="0,-5,-5,0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#333"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Mensagem -->
                                        <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                                   Text="{Binding Message}" Foreground="#CCC" FontSize="12" 
                                                   TextWrapping="Wrap" Margin="0,4,0,0" LineHeight="16"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Aviso de Vazio -->
                <StackPanel x:Name="PanelEmpty" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="&#x1F515;" FontSize="40" HorizontalAlignment="Center" Foreground="#333"/>
                    <TextBlock Text="Nenhuma notifica&#xE7;&#xE3;o" Foreground="#555" Margin="0,10,0,0"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>