<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="KitLugia.GUI.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#CC000000" Panel.ZIndex="99999">
        <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="{DynamicResource AccentColor}" BorderThickness="1" 
                Width="300" Height="120" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                
                <!-- Loading Spinner -->
                <Viewbox Width="40" Height="40" Margin="0,0,0,15">
                    <Grid>
                        <Ellipse Stroke="{DynamicResource AccentColor}" StrokeThickness="3" Fill="Transparent" 
                                Width="40" Height="40" Opacity="0.3"/>
                        <Ellipse Stroke="{DynamicResource AccentColor}" StrokeThickness="3" Fill="Transparent" 
                                Width="40" Height="40" StrokeDashArray="94 31" StrokeDashOffset="0">
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="StrokeDashOffset" 
                                                           From="0" To="125" Duration="0:0:1" 
                                                           RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </Grid>
                </Viewbox>
                
                <!-- Loading Text -->
                <TextBlock x:Name="LoadingText" Text="Processando..." 
                          Foreground="White" FontSize="14" HorizontalAlignment="Center" 
                          VerticalAlignment="Center" TextAlignment="Center" Margin="20,0"/>
                          
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
