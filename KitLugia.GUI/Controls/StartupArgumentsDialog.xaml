<Window x:Class="KitLugia.GUI.Controls.StartupArgumentsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adicionar Argumentos de Inicialização"
        Width="600"
        Height="350"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e1e"
        Foreground="White">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078d4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Título -->
        <TextBlock Grid.Row="0" 
                   Text="Adicionar Argumentos de Inicialização" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>
        
        <!-- Caminho do executável -->
        <TextBlock Grid.Row="1" 
                   Text="Caminho do Executável:" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" 
                 Name="TxtExecutablePath" 
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 Height="60"
                 Margin="0,0,0,15"/>
        
        <!-- Argumentos -->
        <TextBlock Grid.Row="3" 
                   Text="Argumentos Adicionais (opcional):" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="4" 
                 Name="TxtArguments" 
                 Height="50"
                 Margin="0,0,0,15"/>
        
        <!-- Resultado final -->
        <TextBlock Grid.Row="5" 
                   Text="Resultado Final:" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        <TextBox Grid.Row="6" 
                 Name="TxtFinalResult" 
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 Height="60"
                 Margin="0,0,0,15"/>
        
        <!-- Botões -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Name="BtnCancel" 
                    Content="Cancelar" 
                    Width="100"
                    Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>
            <Button Name="BtnOK" 
                    Content="Confirmar" 
                    Width="100"
                    Click="BtnOK_Click"/>
        </StackPanel>
    </Grid>
</Window>
