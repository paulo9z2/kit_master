<UserControl x:Class="KitLugia.GUI.Controls.OptionsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350">
    
    <Border Background="#2A2A2A" 
            CornerRadius="8" 
            BorderBrush="#3E3E3E" 
            BorderThickness="1">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock x:Name="TitleText" Text="Preset" FontSize="18" FontWeight="Bold" Foreground="#FFD700"/>
                <TextBlock x:Name="DescriptionText" Text="Descri&#xE7;&#xE3;o" FontSize="12" Foreground="#AAA" Margin="0,5,0,0"/>
                <TextBlock Text="Selecione as otimiza&#xE7;&#xF5;es:" FontSize="11" Foreground="#888" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Lista de op&#xE7;&#xF5;es -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                <ItemsControl x:Name="OptionsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#333333" 
                                    Padding="12" 
                                    Margin="0,3" 
                                    CornerRadius="5"
                                    Cursor="Hand">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3E3E3E"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <CheckBox Grid.Column="0" 
                                             IsChecked="{Binding IsSelected}" 
                                             Margin="0,0,12,0"
                                             VerticalAlignment="Center"
                                             Width="24"
                                             Height="24"
                                             FontSize="16"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#AAA" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Bot&#xF5;es -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button x:Name="CancelButton" 
                        Content="Cancelar" 
                        Width="90" 
                        Height="32" 
                        Margin="0,0,8,0"
                        Background="#3E3E3E" 
                        Foreground="White" 
                        BorderThickness="0"
                        FontSize="12"
                        Cursor="Hand"
                        Click="CancelButton_Click"/>
                <Button x:Name="ApplyButton" 
                        Content="Aplicar" 
                        Width="90" 
                        Height="32" 
                        Background="#FFD700" 
                        Foreground="Black" 
                        BorderThickness="0"
                        FontSize="12"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
