<UserControl x:Class="KitLugia.GUI.Controls.ModalDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    
    <Grid Background="#E0000000">
        <Border Background="#1A1A1A" 
                CornerRadius="12" 
                BorderBrush="#333" 
                BorderThickness="1"
                MaxWidth="{Binding DialogMaxWidth, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=600}"
                MaxHeight="{Binding DialogMaxHeight, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue=700}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Padding="0">
            <Grid>
                <!-- Header com X para fechar -->
                <Grid Height="50" VerticalAlignment="Top">
                    <Border Background="#252525" CornerRadius="12,12,0,0">
                        <Grid Margin="15,0,0,0">
                            <TextBlock x:Name="TitleText"
                                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='T&#xCD;TULO'}" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="{DynamicResource AccentColor}"/>
                            
                            <Button x:Name="BtnClose"
                                    Content="&#x2715;" 
                                    Width="36" Height="36"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,0,0,0"
                                    Background="Transparent" 
                                    Foreground="#888" 
                                    FontWeight="Bold" 
                                    FontSize="18"
                                    Cursor="Hand" 
                                    Click="BtnClose_Click"
                                    BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="18">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bdr" Property="Background" Value="#FF4444"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
                
                <!-- Conte&#xFA;do -->
                <ScrollViewer x:Name="ContentScrollViewer"
                              VerticalScrollBarVisibility="Auto" 
                              Margin="0,50,0,0">
                    <ContentPresenter x:Name="ContentPresenter"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
