<Window x:Class="KitLugia.GUI.Windows.PresetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Otimiza&#xE7;&#xF5;es do Preset" 
        Height="600" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#1E1E1E"
        WindowStyle="SingleBorderWindow">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="PresetTitle" Text="Preset" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentColor}"/>
            <TextBlock x:Name="PresetDescription" Text="Descri&#xE7;&#xE3;o" FontSize="14" Foreground="#CCC" Margin="0,5,0,0"/>
            <TextBlock Text="Selecione as otimiza&#xE7;&#xF5;es que deseja aplicar:" FontSize="12" Foreground="#888" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Lista de otimiza&#xE7;&#xF5;es -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="OptimizationsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2A2A2A" Padding="15" Margin="0,5" CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox Grid.Column="0" 
                                         IsChecked="{Binding IsSelected}" 
                                         Margin="0,0,15,0"
                                         VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#AAA" Margin="0,3,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Bot&#xF5;es -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#3E3E3E" 
                    Foreground="White" 
                    BorderThickness="0"
                    Click="BtnCancel_Click"/>
            <Button Content="Aplicar Selecionados" 
                    Width="150" 
                    Height="35" 
                    Background="{DynamicResource AccentColor}" 
                    Foreground="White" 
                    BorderThickness="0"
                    Click="BtnApply_Click"/>
        </StackPanel>
    </Grid>
</Window>
