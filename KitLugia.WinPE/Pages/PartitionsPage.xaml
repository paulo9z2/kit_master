<Page x:Class="KitLugia.WinPE.Pages.PartitionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgDarkBrush}"
      Title="Gerenciador de Partições">
    <ScrollViewer Padding="24">
        <StackPanel>
            <TextBlock Text="Gerenciador de Partições" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="Operações via diskpart (compatível com WinPE/ValOS)" 
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0 4 0 24"/>

            <Border Style="{StaticResource CardBorder}" Margin="0 0 0 16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Listar Volumes" Style="{StaticResource SectionTitle}" Margin="0 0 0 8"/>
                    <Button Grid.Row="1" Grid.Column="0" Content="🔄  Atualizar Lista" Style="{StaticResource AccentButton}"
                            Click="BtnRefresh_Click" Width="160" Margin="0 0 12 0"/>
                    <Button Grid.Row="1" Grid.Column="1" Content="📋  Copiar Log" Style="{StaticResource AccentButton}"
                            Click="BtnCopyLog_Click" Width="140"/>
                    <ListBox x:Name="VolumeList" Grid.Row="2" Grid.ColumnSpan="2" Height="200" Margin="0 8 0 0"
                             Background="{StaticResource BgMediumBrush}" BorderThickness="0"
                             Foreground="{StaticResource TextPrimaryBrush}" FontFamily="Consolas" FontSize="12"/>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorder}" Margin="0 0 0 16">
                <WrapPanel>
                    <Button Content="📝  Criar Partição" Style="{StaticResource AccentButton}"
                            Click="BtnCreatePartition_Click" Margin="0 0 8 8"/>
                    <Button Content="🗑️  Deletar Partição" Style="{StaticResource DangerButton}"
                            Click="BtnDeletePartition_Click" Margin="0 0 8 8"/>
                    <Button Content="💿  Formatar Volume" Style="{StaticResource AccentButton}"
                            Click="BtnFormat_Click" Margin="0 0 8 8"/>
                    <Button Content="🔤  Atribuir Letra" Style="{StaticResource AccentButton}"
                            Click="BtnAssignLetter_Click" Margin="0 0 8 8"/>
                    <Button Content="🔤  Remover Letra" Style="{StaticResource AccentButton}"
                            Click="BtnRemoveLetter_Click" Margin="0 0 8 8"/>
                    <Button Content="🔍  Verificar Erros" Style="{StaticResource AccentButton}"
                            Click="BtnCheckDisk_Click" Margin="0 0 8 8"/>
                    <Button Content="🧹  Clean Disk" Style="{StaticResource DangerButton}"
                            Click="BtnCleanDisk_Click"/>
                </WrapPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Log de Operações" Style="{StaticResource SectionTitle}" Margin="0 0 0 8"/>
                    <TextBox x:Name="LogBox" Height="150" IsReadOnly="True"
                             FontFamily="Consolas" FontSize="11"
                             Background="{StaticResource BgMediumBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderThickness="0" VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
